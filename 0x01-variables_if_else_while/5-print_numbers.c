#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - print a single digit of base 10
 *
 * Return: Always return 0 (Success)
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf("\n");

	return (0);
}
